name: Checks

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

on:
  push:
    branches:
      - "main"
      - "dev"
    paths-ignore:
      - "**/*.md"
  pull_request:
    branches:
      - "main"
      - "dev"
    paths-ignore:
      - "**/*.md"

jobs:
  fmt:
    name: Format
    runs-on: ubuntu-latest
    steps:
      - name: Checkout sources
        uses: actions/checkout@v4

      - name: Install Rust-stable
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.89
          components: rustfmt

      - name: Check formatting
        run: |
          cargo fmt --all -- --check

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout sources
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install libssl-dev pkg-config llvm clang libclang-dev binfmt-support libxcursor-dev libx11-dev libasound2-dev libpulse-dev libwayland-dev libxkbcommon-dev

      - name: Install Rust-stable
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.89
          components: rustfmt

      - name: Cache
        uses: Swatinem/rust-cache@v2

      - name: Run Moly tests
        run: |
          cargo test --all-features

      - name: Run Moly Kit tests
        run: |
          cargo test --all-features --package moly-kit

  check-patches:
    name: Check Patches
    runs-on: ubuntu-latest
    steps:
      - name: Checkout sources
        uses: actions/checkout@v4

      - name: Forbid local patches in Cargo.toml
        run: |
          awk '/^\s*#/ {next} /^\s*\[patch/ {in_patch=1; next} /^\s*\[/ {in_patch=0} in_patch && /path\s*=/ {print "Error: patch with path found at line " NR ": " $0; exit 1}' Cargo.toml
